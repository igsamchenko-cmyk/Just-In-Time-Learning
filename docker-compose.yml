services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:////data/learning_mvp.db
      HOST: 0.0.0.0
      PORT: 8000
    volumes:
      - learning_data:/data
    restart: unless-stopped

volumes:
  learning_data:
